<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="container relative">
        <% if(error && error.length > 0) { %>
            <div class="alert alert-danger fixed top-0 left-1/2 transform -translate-x-1/2 w-96 text-center py-3 px-4 bg-red-500 text-white rounded-b-lg shadow-lg z-50" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0) { %>
            <div class="alert alert-success fixed top-0 left-1/2 transform -translate-x-1/2 w-96 text-center py-3 px-4 bg-green-500 text-white rounded-b-lg shadow-lg z-50" role="alert">
                <%= success %>
            </div>
        <% } %>
        <div class="main h-screen w-full flex justify-between items-center">
            <div class="left w-[50%] h-full">
                <div class="form flex flex-col items-center justify-center h-full">
                    <h3 class="mb-5 text-2xl underline">Create your Account</h3>
                    <form action="/createUser" method="post">
                        <div class="flex flex-col items-center justify-center gap-5">
                            <input class="border px-3 py-2 rounded-lg border-gray-600" type="text" placeholder="Username" name="username" required>
                            <input class="border px-3 py-2 rounded-lg border-gray-600" type="email" placeholder="Email" name="email" required>
                            <input class="border px-3 py-2 rounded-lg border-gray-600" type="password" placeholder="Password" name="password" required>
                            <input type="submit" value="Register" class="bg-blue-500 text-white px-4 py-2 rounded-lg cursor-pointer">
                            <p class="text-sm">Already have an account? <a href="/login" class="text-blue-500">Login</a></p>
                        </div>
                    </form>
                </div>
            </div>
            <div class="right h-full">
                    <img class="h-full w-full object-contain" src="/images/cartoon.jpeg" alt="Cartoon holding a board"/>
            </div>
        </div>
    </div>
</body>
</html>